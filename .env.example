# Callboard configuration
#
# Base config:  ~/.callboard/.env  (created automatically on first run)
# Local override: <project-root>/.env (optional, overrides base config)
#
# On startup, callboard loads ~/.callboard/.env first, then the project-root
# .env overrides any matching values. This means:
#   - Put default/shared config in ~/.callboard/.env
#   - Put local dev overrides in the project-root .env
#
# To get started, copy this file:
#   cp .env.example ~/.callboard/.env
# Then edit ~/.callboard/.env and set AUTH_PASSWORD.

# Authentication password for the application (required)
# If empty, not possible to login
AUTH_PASSWORD=

# Port for the application (defaults to 8000)
PORT=8000

# Log level for backend output (error, warn, info, debug). Default: info
LOG_LEVEL=info

# Session cookie name (optional, defaults to "callboard_session")
# Override to avoid collisions if running multiple apps on localhost
# SESSION_COOKIE_NAME=

# Agent workspaces directory (optional, defaults to ~/.callboard/agent-workspaces)
# CALLBOARD_WORKSPACES_DIR=

# ── Dev-only settings (typically kept in project-root .env) ──────────

# Dev ports for frontend UI and backend server
# DEV_PORT_UI=3000
# DEV_PORT_SERVER=3002